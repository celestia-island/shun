# Demo notice

This document is the second entry on the ShunDemo license page. It
exercises the multi-document license flow: shells render it after the
main agreement, paged behind a prev/next control.

## What the demo payload bundles

- the `shun-demo` application binary staged by `just demo-payload`;
- a `bin/` folder with the entry point the shortcut targets;
- a `data/` folder written in place for portable mode.

## Third-party notices

Placeholder only — a real product would list the runtime, UI, and
compression libraries it ships, each with its license reference:

- Tauri and the web runtime it carries (placeholder entry);
- the hikari UI components rendered by the shell (placeholder entry);
- the zstandard compression library used by the payload archive
  (placeholder entry).

Nothing in this notice is a real attribution; replace it with the
actual third-party notices of your product.
